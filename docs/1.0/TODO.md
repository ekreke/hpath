# TODO — Current Iteration

Iteration target: **T1 Workspace skeleton + gRPC contract + server skeleton with mock mode**

## Working notes

- Status: not started.
- Entry state: repo has only `docs/` and `milestones.md` (Phase 0 complete). SPEC reordered to client-first / mock-first (see SPEC "Execution Strategy").
- T1 scope: pnpm workspace root (`packages/server`, `packages/desktop` placeholder), `proto/hpath.proto` with all 1.0 RPCs finalized once, generated TS types, server skeleton with echo impls + `--mock` mode (in-memory seed data, scripted RunCase event stream, synthetic artifacts).
- Next up after T1: T10 desktop skeleton (mock-backed).
- Code comments in English (architecture invariant).

## Blockers

None.
