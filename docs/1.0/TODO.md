# TODO — Current Iteration

Iteration target: **T1 Workspace skeleton + gRPC contract**

## Working notes

- Status: not started.
- Entry state: repo has only `docs/` and `milestones.md` (Phase 0 complete).
- T1 scope: pnpm workspace root (packages/server, packages/desktop placeholder), `proto/hpath.proto` with all 1.0 RPCs, server skeleton with echo implementations, TS type generation from proto.
- Next up after T1: T2 compose topology.

## Blockers

None.
