# Context for docker/server.Dockerfile (repository root). Keep the build
# context small: only manifests, proto and server-side packages are needed.
.git
.github
**/node_modules
**/dist
**/target
docs
docker
# PRD fixtures seed the real-mode database; the demo-app SUT stays out.
fixtures
!fixtures/prds
packages/desktop
*.md
.DS_Store
