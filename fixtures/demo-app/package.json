{
  "name": "hpath-demo-app",
  "private": true,
  "version": "0.1.0",
  "description": "Three-way aligned demo SUT for HPath (SPEC T4): UI / HTTP / gRPC serve one seeded balance value.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@grpc/grpc-js": "1.14.4",
    "@grpc/proto-loader": "0.7.15",
    "@grpc/reflection": "1.0.4"
  }
}
